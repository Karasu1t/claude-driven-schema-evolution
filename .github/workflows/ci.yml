name: CI - PR Pipeline (UT → E2E)

on:
  pull_request:
    branches:
      - dev
      - main

jobs:
  unit-test:
    if: github.repository_owner == 'Karasu1t'
    uses: ./.github/workflows/unit_test.yml
    with:
      pr_number: ${{ github.event.pull_request.number }}

  e2e-test:
    if: github.repository_owner == 'Karasu1t'
    needs: unit-test
    uses: ./.github/workflows/e2e_test.yml
    secrets: inherit
    with:
      pr_number: ${{ github.event.pull_request.number }}
